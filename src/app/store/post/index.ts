import * as fromPost from './post.reducer';
import {ActionReducerMap} from "@ngrx/store";

export interface PostState {
  // post: fromPost.PostState;
}

export const reducers: ActionReducerMap<PostState> = {
  // post: fromPost.reducers.post,
}
